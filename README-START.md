# Запуск приложения

## Быстрый старт (рекомендуется)

Для запуска бэкенда и Telegram бота одновременно используйте:

```bash
npm run dev:all
```

Эта команда запустит:

- ✅ Backend API сервер на `http://localhost:3000`
- ✅ Telegram бота для обработки запросов

## Альтернативные команды

### Только бэкенд:

```bash
npm run dev
```

### Только бот:

```bash
npm run bot
```

## Проверка работы

После запуска `npm run dev:all` вы увидите вывод обоих процессов:

```
[BACKEND] ✓ Ready in 5.4s
[BACKEND] Local: http://localhost:3000
[BOT] 🤖 Telegram bot started!
[BOT] 📡 API_URL: http://localhost:3000/api
[BOT] ✅ API connection successful
```

## Остановка

Нажмите `Ctrl+C` для остановки обоих процессов.
